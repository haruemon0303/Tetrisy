# Tetrisy

ブラウザで動作するブロックパズルゲーム（テトリス風）。外部依存なしの純粋なHTML/CSS/JavaScriptで実装されています。

## 特徴

- 📱 **完全レスポンシブ対応** - PCからスマートフォンまで快適にプレイ可能
- 🎮 **直感的な操作** - キーボードとタッチ操作の両方に対応
- 🎯 **7バッグ方式** - 公平なブロック生成システム
- 🚀 **依存なし** - CDNや外部ライブラリ不要
- 🔒 **プライバシー重視** - データ収集なし、外部通信なし

## ゲームルール

### 目的
落下してくるブロックを回転・移動させて、横一列を揃えて消去します。ラインを消すとスコアが加算され、一定ライン数でレベルアップして難易度が上がります。

### ブロックの種類
7種類のテトロミノ（I、O、T、S、Z、J、L）がランダムに出現します。

### スコアリング
- 1ライン消去: 100点 × レベル
- 2ライン同時消去: 300点 × レベル
- 3ライン同時消去: 500点 × レベル
- 4ライン同時消去: 800点 × レベル
- ハードドロップ: 1マスにつき2点

### レベルアップ
10ライン消去ごとにレベルが1上がり、ブロックの落下速度が速くなります。

## 操作方法

### キーボード操作
- **← →** : ブロックを左右に移動
- **↑** : ブロックを時計回りに回転
- **↓** : ブロックを高速落下（ソフトドロップ）
- **Space** : ブロックを即座に落下（ハードドロップ）
- **P** : 一時停止/再開

### タッチ操作（モバイル）
画面下部のボタンで操作できます：
- **◀ ▶** : 左右移動
- **🔄** : 回転
- **▼** : 高速落下
- **DROP** : 即座に落下

## プレイ方法

### ローカルで遊ぶ
1. このリポジトリをクローンまたはダウンロード
   ```bash
   git clone https://github.com/yourusername/Tetrisy.git
   cd Tetrisy
   ```

2. `index.html` をブラウザで開く
   - ダブルクリックで開く、または
   - 右クリック → 「プログラムから開く」→ お好みのブラウザを選択

### GitHub Pagesで公開する

#### 方法1: リポジトリ設定から有効化
1. GitHubのリポジトリページに移動
2. **Settings**（設定）タブをクリック
3. 左サイドバーの **Pages** をクリック
4. **Source** セクションで以下を選択：
   - Branch: `main` (または `master`)
   - Folder: `/ (root)`
5. **Save** をクリック
6. 数分後、`https://yourusername.github.io/Tetrisy/` でアクセス可能になります

#### 方法2: コマンドラインから
```bash
# 変更をコミット
git add .
git commit -m "Add Tetrisy game"

# メインブランチにプッシュ
git push origin main

# GitHub Pagesブランチを作成（必要に応じて）
git checkout -b gh-pages
git push origin gh-pages
```

その後、リポジトリの Settings → Pages で `gh-pages` ブランチを選択します。

## 技術仕様

- **フロントエンド**: HTML5, CSS3, JavaScript (ES6+)
- **描画**: Canvas API
- **外部依存**: なし
- **ブラウザ対応**: モダンブラウザ（Chrome, Firefox, Safari, Edge）

## ファイル構成

```
Tetrisy/
├── index.html      # メインHTMLファイル
├── style.css       # スタイルシート
├── script.js       # ゲームロジック
└── README.md       # このファイル
```

## セキュリティとプライバシー

- 外部サーバーへのデータ送信なし
- 個人情報の収集なし
- クッキーやローカルストレージの使用なし
- 完全にクライアントサイドで動作

## ライセンス

このプロジェクトはオープンソースです。自由に使用、改変、配布できます。

## 開発

このゲームは教育目的で作成されました。改善提案やバグ報告は歓迎します。

### 貢献方法
1. このリポジトリをフォーク
2. 新しいブランチを作成 (`git checkout -b feature/amazing-feature`)
3. 変更をコミット (`git commit -m 'Add some amazing feature'`)
4. ブランチにプッシュ (`git push origin feature/amazing-feature`)
5. プルリクエストを作成

## クレジット

オリジナルのテトリスは1984年にアレクセイ・パジトノフによって作成されました。このプロジェクトはテトリスにインスパイアされたオリジナル実装です。

---

**Enjoy playing Tetrisy!** 🎮✨
